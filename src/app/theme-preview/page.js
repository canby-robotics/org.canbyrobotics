import { BuildBrightReference } from "./BuildBrightReference";

export const metadata = {
  title: "Build Bright Reference | Canby Robotics",
  description:
    "Review the Build Bright palette, semantic roles, components, and seasonal brand boundary.",
  robots: {
    follow: false,
    index: false,
  },
};

export default function BuildBrightReferencePage() {
  return <BuildBrightReference />;
}
